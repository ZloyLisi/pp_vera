<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/vera_shop_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/vera_shop_icon"
        android:supportsRtl="true"
        android:theme="@style/MyTheme"
        tools:targetApi="31">
        <activity
            android:name=".SpravkaActivity"
            android:exported="false"
            android:label="@string/sprafka" />
        <activity
            android:name=".OProgrammeActivity"
            android:exported="false"
            android:label="@string/oprogramme"/>
        <activity
            android:name=".ZakazUserActivity"
            android:exported="false" />

        <activity
            android:name=".SpleshScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DetailedActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>